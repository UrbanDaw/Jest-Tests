const { expect } = require("@jest/globals");


// Przykład
test("Co robi", () => {
    expect(funkcja(parametry)).toBe(4);
});

test("Checking if the function is defined", () => {
    expect(function()).toBeDefined()
})

test("Checking if type of budget is number"), () => {
    expect(typeof zmienna ).toBe("string"))
}
